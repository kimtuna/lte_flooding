# LTE Flooding 프로젝트 의존성
# 주의: srsRAN은 별도로 설치해야 합니다 (https://github.com/srsran/srsRAN)

# Python 표준 라이브러리만 사용하므로 추가 패키지가 필요 없습니다.
# 하지만 향후 확장을 위해 필요한 패키지들을 나열합니다.

# UHD (USRP Hardware Driver) Python 바인딩 (선택사항)
# uhd>=4.0.0

# 로깅 및 유틸리티 (표준 라이브러리로 충분)
# logging, threading, subprocess 등은 Python 표준 라이브러리

